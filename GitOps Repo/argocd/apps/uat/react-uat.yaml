# ============================================================
# Child Application — java-app7 (UAT)
# Purpose: Deploys app7 to the uat EKS cluster
# Sync: Manual (team lead triggers)
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: react-app-uat
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: uat
    app: java-app7
spec:
  project: uat
  source:
    repoURL: 'https://github.com/vishutyagi0210/demo-argocd.git'
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
        - values.yaml
        - ../applications/uat/react-app-uat-values.yaml
  destination:
    # TODO: replace with cluster 2 API server URL after cluster 2 is registered in ArgoCD
    server: https://REPLACE_WITH_CLUSTER2_API_URL
    namespace: react-app-uat
  syncPolicy:
    # Manual sync only
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 2
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
