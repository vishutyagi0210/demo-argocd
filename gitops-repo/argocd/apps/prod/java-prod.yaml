apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: java-app-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: prod
    app: java-app6
spec:
  project: prod
  source:
    repoURL: 'https://github.com/vishutyagi0210/demo-argocd.git'
    targetRevision: HEAD
    path: gitops-repo/helm
    helm:
      valueFiles:
        - values.yaml
        - ../applications/prod/java-app-prod-values.yaml
  destination:
    # In-cluster mode — both ArgoCD and the workloads live on the same cluster
    server: https://kubernetes.default.svc
    namespace: java-app-prod
  syncPolicy:
    # Manual sync only — auto-sync explicitly disabled for safety
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
