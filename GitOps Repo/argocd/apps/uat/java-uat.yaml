apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: java-app-uat
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: uat
    app: java-app6
spec:
  project: uat
  source:
    repoURL: https://github.com/YOUR_ORG/spring-boot-realworld-example-app.git
    targetRevision: HEAD
    path: helm/platform-app
    helm:
      valueFiles:
        - values.yaml
        - ../../applications/app6/uat-values.yaml
  destination:
    server: https://uat-eks-endpoint.eks.amazonaws.com
    namespace: java-app6
  syncPolicy:
    # Manual sync only
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 2
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
